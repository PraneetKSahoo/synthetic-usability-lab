# Screenshots

Drop UI screenshots here and reference them from the main [README](../../README.md). Suggested filenames (used as placeholders there already):

- `dashboard.png` — the "Live Testing Sandbox" tab with a URL/task entered
- `replay-step.png` — a single step card from the replay view (screenshot + internal monologue + UX finding)
- `comparative-matrix.png` — the executive benchmark table comparing personas
- `persona-editor.png` — the Persona Customizer & Manager tab

Once added, remove this file (or leave it — it won't render anywhere except this folder).
